# AutoTranslate 使用教程

> 把一篇英文论文变成「保留原排版」的中文 PDF，只需要双击两次。
> 本教程面向第一次使用的用户，从安装到进阶技巧一步步讲清楚。

## 目录

- [一、首次安装](#一首次安装只需做一次)
- [二、配置大模型](#二配置大模型两种方式推荐界面配置)
- [三、翻译一篇论文](#三翻译一篇论文三种方式任选)
- [四、查看进度与下载](#四查看进度与下载)
- [五、内置阅读器：批注与划词解释](#五内置阅读器批注与划词解释)
- [六、AI 论文概括与思维导图](#六ai-论文概括与思维导图)
- [七、删除历史记录](#七删除历史记录)
- [八、任务失败了怎么办](#八任务失败了怎么办)
- [九、常用配置详解](#九常用配置详解)
- [十、数据都存在哪 / 数据安全](#十数据都存在哪--数据安全)
- [十一、费用与性能参考](#十一费用与性能参考)
- [十二、常见问题 FAQ](#十二常见问题-faq)
- [十三、更新与卸载](#十三更新与卸载)

---

## 一、首次安装（只需做一次）

### 1. 环境要求

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10/11（macOS、Linux 也可用命令行方式启动） |
| Python | **3.10 及以上**（[python.org](https://www.python.org/downloads/) 下载，安装时务必勾选 **"Add Python to PATH"**） |
| 磁盘空间 | 约 2GB（含虚拟环境、字体与排版分析模型缓存） |
| 网络 | 需要访问 PyPI（首次下载依赖）、arXiv（下载论文）、大模型服务商 |

确认 Python 版本：按 `Win+R` 输入 `cmd` 回车，在黑色窗口输入 `python --version`，显示 `Python 3.10` 以上即可。

### 2. 安装与启动（双击即可）

**双击项目目录里的 `论文助手.bat`（桌面窗口模式，推荐）或 `start.bat`（浏览器模式）**，就这一步：

1. 首次运行会自动创建虚拟环境、安装依赖（约 1-3 分钟，取决于网速）
2. 自动生成 `.env` 配置文件并弹出记事本——先关掉它，去下一步配置模型
3. 再次双击 `start.bat`，浏览器自动打开 **http://127.0.0.1:8000**

> 以后每次使用：**双击 `论文助手.bat` → 弹出独立应用窗口**（推荐，无黑窗无浏览器），
> 或双击 `start.bat` → 浏览器自动打开。关闭窗口/黑窗即停止服务。
>
> **打包成免安装 exe**：运行 `.venv\Scripts\pyinstaller.exe 论文助手.spec --noconfirm`，
> 产物在 `dist/论文助手/`，双击其中的 **`论文助手.exe`** 即可使用（可把整个文件夹拷给别人）。

<details>
<summary>命令行方式启动（macOS / Linux / 喜欢终端的用户）</summary>

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
.venv/bin/python run.py
```

</details>

---

## 二、配置大模型（两种方式，推荐界面配置）

翻译质量和 AI 功能取决于你配置的大模型。**两种方式二选一**：

### 方式一（推荐）：界面内配置，全程不用碰配置文件

1. 打开网页后，点击**左侧底部的模型状态栏**（显示"未配置模型"或模型名的地方）
2. 在弹出的「模型设置」窗口中：
   - **翻译服务**选「自定义大模型」
   - **服务商**下拉选择：智谱 GLM / DeepSeek / OpenAI / Kimi / 自定义——API 地址会自动填好
   - **API Key** 粘贴你的 Key
   - **模型**点「列表」按钮从服务商一键拉取，下拉选择即可（也可以手动输入）
3. 点 **「测试连接」**，提示连接成功后点 **「保存设置」**

保存后**即时生效**，正在进行的翻译不受影响，新任务使用新模型。

> **没有 Key？** 去 [智谱开放平台](https://open.bigmodel.cn/) 或 [DeepSeek 开放平台](https://platform.deepseek.com/) 注册，
> 新用户一般有免费额度；`glm-4.5-flash`、`deepseek-chat` 等模型翻译一篇论文只需几分钱。
> 也可以在模型设置里选「免费试用服务」零成本体验（速度慢、质量一般）。

### 方式二：`.env` 文件（首次双击 start.bat 时自动弹出）

填入三行关键配置并保存：

```ini
LLM_API_KEY=sk-xxxxxxxxxxxxxxxx        # 你的 Key
LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
LLM_MODEL=glm-4.5-flash
```

常见服务商对照（任选其一，均兼容 OpenAI 接口）：

| 服务商 | LLM_BASE_URL | 推荐模型 | 获取 Key |
|---|---|---|---|
| 智谱 GLM | `https://open.bigmodel.cn/api/paas/v4` | `glm-4.5-flash`（便宜） | open.bigmodel.cn |
| DeepSeek | `https://api.deepseek.com` | `deepseek-chat` | platform.deepseek.com |
| OpenAI | `https://api.openai.com/v1` | `gpt-4o-mini` | platform.openai.com |
| 月之暗面 Kimi | `https://api.moonshot.cn/v1` | `moonshot-v1-8k` | platform.moonshot.cn |
| 阿里通义 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `qwen-plus` | bailian.aliyun.com |

改完 `.env` 后**关掉服务窗口，重新双击 `start.bat`** 生效。

> **优先级**：界面「模型设置」保存的配置（存于 `model_config.json`）**优先于** `.env`。
> `.env` 只作为初始默认值。日常换模型、调参数都在界面里操作即可。

---

## 三、翻译一篇论文（三种方式任选）

打开网页后，左侧是导航栏（**翻译论文 / 翻译任务 / 使用说明**），翻译页顶部有三个来源切换标签。

### 方式 A：联网搜索（最方便）

1. 切到「**联网搜索**」标签
2. 输入论文标题或关键词（**支持中文**，系统自动译成英文检索），例如：
   - `vision transformer`
   - `大语言模型 幻觉`
   - `attention is all you need`
3. 回车或点「搜索」，几秒后出现结果卡片（标题 / 作者 / 年份 / 来源）
4. 找到目标论文，点右侧 **「翻译」** 按钮
   - 标注「无开放获取 PDF」的论文没有免费全文，按钮不可点
5. 系统自动完成：下载 PDF → 排队 → 翻译 → 生成译文，全程无需操作

搜索覆盖 **arXiv**（200 万+ 论文）和 **Semantic Scholar**（数亿篇，含跨库元数据）。

### 方式 B：上传本地 PDF

切到「**本地上传**」，把 `.pdf` 文件**拖拽**到虚线框（或点击框选择文件）：

- 仅支持文字版 PDF（扫描件暂不支持，会明确报错）
- 适合：出版社官网下载的付费论文、导师给的文件、arXiv 之外的一切 PDF

### 方式 C：粘贴 arXiv ID 或链接

切到「**链接 / arXiv ID**」，粘贴任一格式后点「获取并翻译」：

```
1706.03762                                # arXiv 编号
arXiv:2005.14165v2                        # 带版本号
https://arxiv.org/abs/1706.03762          # arXiv 摘要页
https://arxiv.org/pdf/1706.03762          # arXiv PDF 直链
https://www.some-site.org/paper.pdf       # 任意网站的 PDF 直链
```

> **小技巧**：arXiv 论文页面上那个大大的编号（如 `arXiv:2312.00752`）直接复制进来就行。

### 关于「同时生成双语对照版 PDF」

翻译页底部有一个勾选框（默认勾选）：

- **勾选**：产出两个 PDF——纯译文 + 左英右中对照版
- **不勾选**：只产出纯译文，速度稍快
- 每个任务独立勾选，也可在「模型设置」里改默认值

---

## 四、查看进度与下载

进入左侧「**翻译任务**」视图，列表每 3 秒自动刷新。每个任务卡片包含：

- **状态标签**：`排队中` → `下载中` → `翻译中` → `完成` / `失败` / `已取消`
- **当前阶段**：如「Translate Paragraphs 45%」（翻译段落进行到 45%）
- **进度条**：蓝色流动条 = 进行中；出结果后原位变成下载按钮

任务完成后卡片上的按钮：

| 按钮 | 内容 | 说明 |
|---|---|---|
| **阅读** | 打开内置阅读器 | 见第五节，可批注/划词解释 |
| **AI 概括** | AI 阅读弹窗 | 见第六节 |
| **译文** | 纯中文译文 PDF | 版式与原论文一致，日常阅读用这个 |
| **双语对照** | 左英右中对照 PDF | 精读、核对术语时用 |
| **原文** | 原始 PDF | 对照参考 |
| 🗑 删除 | 删除记录及文件 | 见第七节 |

> **翻译要多久？** 一篇 10 页论文通常 **2-5 分钟**（取决于模型速度）；
> 3 页短文约 1 分半。多篇论文同时提交会自动排队。

**中途反悔？** 进行中的任务点「取消」即可终止（已消耗的 token 费用不退）。

---

## 五、内置阅读器：批注与划词解释

点任务卡片的 **「阅读」**，进入全屏阅读器。**不用下载任何软件**，浏览器里直接精读：

### 界面布局

- **顶部工具栏**：论文标题、原文/译文/双语对照切换标签、页码跳转（输入框直接输页码）、缩放按钮
- **左侧主区**：PDF 页面**连续滚动**阅读（像刷网页一样往下滚，页面自动懒加载）
- **右侧面板**：三个页签——「批注」「AI 解释」「目录」

**目录导航**：点「目录」页签显示论文书签章节树（原文一般都有），点章节直接跳页；没有书签的文档会给出提示。

### 添加批注

1. 用鼠标在 PDF 上**选中任意文字**（一句话、一段公式说明、一个术语都行）
2. 选中位置上方弹出小条，点 **「批注」**
3. 右侧出现编辑框（自动带上你选中的原文引用），写下你的理解/疑问/翻译对照
4. 点「保存批注」

批注卡片会显示：所在文档和页码（如「译文 · 第1页」）、引用的原文、你的笔记、时间。

- **跳转**：点批注卡片上的徽章（如「译文 · 第1页」），左侧自动翻到那一页
- **删除**：点卡片右上角垃圾桶图标

批注保存在本地 `outputs/<任务ID>/annotations.json`，重启不丢，删除任务时一并清除。

**导出学习笔记**：批注面板顶部点 **「导出笔记」**，一键把全部批注下载为排版良好的 Markdown 文件（按文档/页码分组：引用原文 + 你的笔记），可直接放进 Obsidian/Typora 等笔记软件。

**AI 助手「小鱼」**：界面右下角的鲸鱼女仆桌宠，点击展开聊天面板。工具用法、论文概念、学习问题随时问，支持连续追问；点扫帚图标清空对话。

### 划词 AI 解释（看不懂的地方随手一点）

1. 选中不懂的内容——**一个术语、半句话、整段都行**（300 字以内）
2. 弹出小条点 **「解释」**
3. 右侧「AI 解释」面板给出中文说明，AI 会**结合这篇论文的上下文**：
   - 术语 → 定义 + 在论文中的作用 + 与其他概念的关系
   - 句子 → 含义解释 + 在论文中的意义
4. 解释末尾的**相关术语**可以继续点击，层层深入

> 例：读 Transformer 论文时选中 "scaled dot-product attention"，
> AI 会解释缩放点积注意力的定义、为什么要除以 √dₖ、以及它和多头注意力的关系。

---

## 六、AI 论文概括与思维导图

任务创建后（**不需要等翻译完成**），点任务卡片上的 **「AI 概括」**，弹出阅读窗口：

### 三个页签

| 页签 | 内容 |
|---|---|
| **内容概括** | 中文摘要，自动分「研究问题 / 方法 / 实验结果 / 结论与贡献」四节 |
| **思维导图** | 论文结构与流程图：中心主题 + 彩色分支（背景/方法/实验/结论）+ 要点节点 |
| **关键词** | 8-10 个核心术语卡片（中英对照），点卡片直接查看 AI 解释 |

### 使用要点

- 生成一次通常 **10-60 秒**（取决于论文长度与模型速度），费用远低于全文翻译
- 概括结果**自动缓存**：第一次生成后，以后打开秒出，不重复花钱
- 缓存存在 `outputs/<任务ID>/summary.json`，删除任务时一并清除
- 适合场景：组会前快速摸清一篇论文、筛选值不值得精读、写综述时批量过论文

---

## 七、删除历史记录

- **删除单条**：任务卡片右侧 🗑 按钮 → 确认后删除记录**及其全部文件**（原文、译文、批注、概括缓存）
- **清空全部**：「翻译任务」视图右上角 **「清空历史」**：
  - 有进行中任务时会提示「进行中的任务保留」，只删已结束的
  - 全部结束时一次清空
- **保护机制**：进行中/排队中的任务不能直接删除，须先「取消」

---

## 八、任务失败了怎么办

任务卡片会直接显示失败原因（红色文字），对照下表处理：

| 错误提示 | 原因与解决 |
|---|---|
| API Key 无效或未授权（401） | Key 填错或已过期。到「模型设置」重新粘贴；若用 .env，检查有无多余空格 |
| 接口不存在（404） | API 地址不对。检查 Base URL 是否少了 `/v1` 或多了多余路径 |
| 请求受限或额度不足（429） | 账户欠费 / 触发限流。充值，或到「模型设置」把 QPS 调小（如 2），稍后重试 |
| 无法连接大模型服务 | 网络问题或 API 地址拼写错误；检查是否需要代理 |
| PDF 没有文本层（扫描件） | 扫描版 PDF 无法翻译/概括，请找文字版（arXiv 上的都是文字版） |
| 下载内容不是有效的 PDF | 链接指向的不是 PDF（可能是网页）；换直链或下载后用本地上传 |
| 该论文没有可下载的 PDF | 搜索结果没有开放获取全文；去出版社官网下载后用本地上传 |
| 服务重启，任务中断 | 正常现象（中途关过服务窗口），重新提交即可 |
| 大模型未返回有效结果 | 偶发（模型输出不稳定），系统已自动重试一次；再点一次即可 |

**查看详细日志**：每个任务的完整运行日志在 `outputs/<任务ID>/translate.log`。

### 术语表（专业翻译必备）

翻译页右下角点 **「术语表」**：每行一条 `英文,中文`（如 `LLM,大语言模型`），保存后对所有新任务生效：

- **译法全文一致**：同一术语不会再出现多种译法
- **保护专有名词**：`Transformer,Transformer` 让模型名不被翻译
- 当前术语在弹窗下方列表可见，可随时清空

---

## 九、常用配置详解

### 「模型设置」弹窗（推荐，保存即时生效）

| 配置项 | 说明 |
|---|---|
| 翻译服务 | 自定义大模型（正式使用）/ 免费试用服务（零成本体验） |
| 服务商 / API 地址 | 选预设自动填；自定义服务填其 OpenAI 兼容地址 |
| API Key | 留空 = 保留原值；界面只显示脱敏形式（`sk-9e***472b`） |
| 模型 | 「列表」按钮一键拉取；也可手动输入任意模型名 |
| 翻译频率 QPS | 每秒翻译请求数。默认 4；报 429 限流就调小（2 或 1） |
| 目标语言 | 译文语言，即改即用（对下一个任务生效） |
| 访问密码 | 开启后局域网访问需输入密码；输入 `none` 关闭；留空保留原值 |

### `.env` 文件（初始默认值）

```ini
TRANSLATE_LANG_IN=en      # 源语言
TRANSLATE_LANG_OUT=zh     # 目标语言
ENABLE_DUAL=true          # 默认是否生成双语对照
TRANSLATE_QPS=4           # 翻译频率
PORT=8000                 # 服务端口
HOST=127.0.0.1            # 监听地址；改成 0.0.0.0 可让局域网其他设备访问
```

改 `.env` 后需重启服务（关窗口再双击 `start.bat`）。

---

## 十、数据都存在哪 / 数据安全

| 内容 | 位置 | 说明 |
|---|---|---|
| 你的模型配置 | `model_config.json` | **含 API Key，勿外传**（已被 .gitignore 排除） |
| 初始配置模板 | `.env` | 同样含 Key，勿外传 |
| 任务历史 | `autotranslate.db` | SQLite，可随时备份 |
| 上传/下载的原文 | `uploads/` | |
| 译文、批注、概括、日志 | `outputs/<任务ID>/` | 每个任务一个独立文件夹 |

**数据安全说明**：

- 这是一个**纯本地服务**：所有数据只存在你自己的电脑上，不上传到任何第三方服务器
- 唯一的外发数据：① 需要翻译/概括的文本会发给**你配置的大模型服务商**（这是功能本身）；② 搜索和下载论文会访问 arXiv / Semantic Scholar
- 批注、任务历史、配置都只在本地；本仓库不包含以上任何数据（.gitignore 已排除）

**彻底清理**：删除 `uploads/`、`outputs/`、`autotranslate.db`、`model_config.json` 即恢复全新状态（`.venv` 不用动）。

---

## 十一、费用与性能参考

| 项目 | 参考（以便宜档模型如 glm-4.5-flash / deepseek-chat 计） |
|---|---|
| 翻译 10 页论文 | 约 0.03-0.1 元 |
| AI 概括一次 | 约 0.005-0.02 元（只读一遍文本） |
| 关键词解释一次 | 约 0.002 元 |
| 中文搜索一次 | 约 0.001 元（只是翻译检索词） |

**省钱技巧**：

- 不需要双语对照就取消勾选，翻译量更小
- 概括有缓存，重复打开不重复计费
- 用「AI 概括」先筛选，只翻译真正值得精读的论文

**速度参考**（QPS=4，单任务）：

| 论文篇幅 | 翻译耗时 | AI 概括 |
|---|---|---|
| 3-4 页短文 | 约 1-2 分钟 | 10-20 秒 |
| 10 页标准论文 | 2-5 分钟 | 20-40 秒 |
| 25 页长文 | 8-15 分钟 | 40-90 秒 |

---

## 十二、常见问题 FAQ

**Q：翻译会破坏公式和图表吗？**
不会。引擎在原 PDF 上做文字层替换，公式、图表、表格、超链接、双栏版式都保留原样。极复杂公式偶有轻微偏移，属已知边界情况。

**Q：能同时翻译多篇吗？**
可以。搜索/上传多篇即可，自动排队，同时最多并行 2 篇；队列任务数显示在侧边栏「翻译任务」旁的角标上。

**Q：搜索中文关键词是什么原理？**
arXiv 的元数据是纯英文，直接搜中文只会得到无关结果。程序检测到中文后，先调用你配置的大模型把关键词译成英文检索词（如「大语言模型 幻觉」→ "large language model hallucination"）再搜索，所以**中文搜索需要已配置 Key**。

**Q：支持哪些论文来源？**
搜索覆盖 arXiv 和 Semantic Scholar；任何网站的 PDF 直链都能通过「链接」入口翻译。IEEE、Elsevier 等付费墙内的论文请自行下载后用「本地上传」。

**Q：扫描版论文为什么不行？**
扫描件是图片不是文字，需要 OCR 技术，当前版本不支持。判断方法：用 PDF 阅读器能不能选中/复制文字，能选中就是文字版。

**Q：AI 概括的摘要可以复制吗？**
可以，概括、解释都是普通网页文本，直接选中复制；思维导图是结构化排版，文字也可选中。

**Q：服务重启后历史还在吗？**
在。任务记录在 SQLite、译文/批注/概括缓存在 outputs/，重启后全部保留；只有"进行中"的任务会标记为中断，重新提交即可。

**Q：换电脑怎么迁移？**
复制整个项目文件夹（可排除 `.venv`），到新电脑双击 `start.bat` 重建环境即可；历史在 `autotranslate.db` 和 `outputs/` 里一并带走。

**Q：想让局域网其他设备（手机/平板）访问怎么办？**
`.env` 中把 `HOST=127.0.0.1` 改为 `HOST=0.0.0.0`，重启后用 `http://你电脑的IP:8000` 访问。注意：仅建议在可信局域网内这样做，服务没有登录鉴权。

**Q：界面图标没显示 / 样式乱了？**
界面图标和 Markdown 渲染使用 jsDelivr CDN，需要能访问外网；公司内网可能拦截。刷新页面（Ctrl+F5）通常可解决。

---

## 十三、更新与卸载

**更新到新版**：覆盖项目文件后，删除 `.venv` 文件夹，重新双击 `start.bat`（自动重装依赖）。

**彻底卸载**：
1. 删除整个项目文件夹
2. 删除 pdf2zh 模型缓存：`C:\Users\你\.cache\` 下的 babeldoc / pdf2zh 相关文件夹
3. 需要保留翻译结果的话，先备份 `outputs/`

---

*遇到本教程未覆盖的问题：查看任务卡片上的错误提示与 `outputs/<任务ID>/translate.log` 日志，或在 GitHub 仓库提 Issue。*
